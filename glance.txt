grep bob /etc/passwd | cut -d ':' -f1,5 | sort | tr ":" " " | column -t
grep 'build job' -lR
ssh-keygen -t rsa
awk -F "." '{print $1}'
grep 'test' test.txt
find /etc -type f -name yum*
/etc/init.d/
cat ~/.bash_profile
export TZ="US/Central"
while read LINE
do
	echo "$LINE"
done < /etc/fstab
